package ru.job4j.oop;

public class Ball {
    public int max(int[] array) {
        int rsl;
        if (array.length > 0) {
            int max = 1;
            rsl = max;
        } else {
            int max = 2;
            rsl = max;
        }
        return rsl;
    }
}
